import React from "react";

// if page can't be found, returns the message below to the user
const NoMatch = () => {
  return <div>Oops, we couldn't find that page.</div>;
};

export default NoMatch;
